export type ProductDTO = {
  badges: string[];
  image?: string;
  title: string;
  collection: string;
  size: string;
  colors: number;
  price: number;
  originalPrice?: number;
  productId: string;
};
